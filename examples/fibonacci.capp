uint64 fib(uint64 n) {
    if (n < 2) {
        return n;
    }

    return fib(n-1) + fib(n-2);
}

uint8 main() {
    print_s("Enter a number");
    uint64 n = input_i();

    print(fib(n));

    return 0;
}
